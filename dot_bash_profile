# ~/.bash_profile
# Executed by bash login shells

# 1) Login environment (PATH, exported vars)
if [ -f "$HOME/.profile" ]; then
  . "$HOME/.profile"
fi

# 2) Bash interactive configuration (aliases, prompt, tooling)
if [ -f "$HOME/.bashrc" ]; then
  . "$HOME/.bashrc"
fi
