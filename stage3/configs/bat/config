# bat Configuration - Stage 3
# Philosophy: Minimal configuration leveraging intelligent defaults
# 
# bat exemplifies the modern CLI philosophy: sensible defaults that
# rarely need override. This configuration only adjusts essential
# preferences while respecting the tool's inherent intelligence.

# Theme selection
# Uses base16 for universal compatibility with other tools
--theme="base16"

# Paging behavior
# Integrates with less for familiar navigation while adding features
--pager="less -RFX"

# Style elements
# Enables all visual enhancements for maximum information density
--style="full"

# Language mappings
# Ensures proper syntax highlighting for common config files
--map-syntax="*.conf:INI"
--map-syntax=".gitignore:Git Ignore"
--map-syntax="*.dockerfile:Dockerfile"
--map-syntax="**/config:YAML"

# Performance optimizations
# Uses tabs for alignment to respect terminal settings
--tabs=4

# Integration features
# Enables decorations even when piping for consistent experience
--decorations=always

# Behavioral defaults
# Shows line numbers for reference and debugging
--number
