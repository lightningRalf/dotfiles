# Safe operations (prompts before destructive actions)
alias rm='rm -i'
alias cp='cp -i'
alias mv='mv -i'

# System info with human-readable output
alias df='df -h'
alias du='du -h'
alias free='free -h'
